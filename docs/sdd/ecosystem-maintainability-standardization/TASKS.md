# Ecosystem Maintainability Standardization — holonight-config

| ID | Task | State | Verification |
|---|---|---|---|
| HOLONIGHT_CONFIG-01 | Raise and verify the public package's C++23 baseline without breaking consumers. | Planned | — |
| HOLONIGHT_CONFIG-02 | Replace the ambiguous user-local `install` task with capability-based build/test tasks; keep system removal centralized. | Planned | — |
| HOLONIGHT_CONFIG-03 | Keep the install-tree consumer test and add an umbrella-stage (`DESTDIR`, prefix `/usr`) consumption check. | Planned | — |
| HOLONIGHT_CONFIG-04 | Document exported targets, ABI expectations, and installed license/header paths. | Planned | — |

Allowed states are `Planned`, `Ready`, `In Progress`, `Done`, `Blocked`, and `Superseded`. Record exact commands
and results before marking a task `Done`.
